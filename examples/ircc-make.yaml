pipeline_name: ircc-make

tasks:
  - name: clone
    run: |
      git clone https://github.com/mirmik/ircc

  - name: cmake
    run: |
      cd ircc
      cmake .

  - name: cmake-build
    run: |
      cd ircc
      cmake --build .

  - name: cmake-tests
    run: |
      cd ircc
      ./runtests
